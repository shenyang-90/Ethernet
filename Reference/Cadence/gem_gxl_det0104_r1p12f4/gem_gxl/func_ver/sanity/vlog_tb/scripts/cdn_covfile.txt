deselect_coverage -module * -all
deselect_coverage -covergroup * -all
select_coverage -module gem... -all
set_expr_scoring -all 
set_refinement_resilience
select_coverage -instance tb_gem.sv_coverage -all
set_toggle_portsonly
set_com -log
select_functional
select_functional -imm_asrt_class_package

//set_expr_coverable_operators -bitwise -conditional -logical_not
//set_assign_scoring
